
<h1> <%= @task.title %></h1>

<%= link_to "Edit this task", edit_task_path(@task) %>

<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
